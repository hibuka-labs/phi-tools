[package]
name = "phi-tools"
version = "0.1.0"
edition = "2024"
description = "General-purpose Agent toolset — local shell, file operations, etc."

[dependencies]
agent-base = { path = "../agent-base" }
async-trait = "0.1"
serde_json = "1"
tokio = { version = "1", features = ["process", "time"] }
tracing = "0.1"

[dev-dependencies]
